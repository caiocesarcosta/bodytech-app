package com.example.login.model.auth

/**
 * Define os tipos possíveis de login no aplicativo.
 */
enum class LoginType {
    /** Login realizado por um usuário comum. */
    USER,
    /** Login realizado por uma empresa. */
    COMPANY
}